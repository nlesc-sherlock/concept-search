You need a Go compiler to run this.

    sudo apt-get install golang

Then

    go run mutinf.go index doctype |
        curl -s -XPOST localhost:9200/_bulk --data-binary @- |
        jq '.took, .errors'
